@model TCS.EF.Entidades.Module

@{
    ViewData["Title"] = SharedLocalizer["modules"].ToString();
    ViewData["PageName"] = "modules_index";
    ViewData["Heading"] = "<i class='subheader-icon fal fa-chart-area'></i> " + SharedLocalizer["modules"].ToString();
}

<div>
    <dl class="row" style="font-size :20px">
        <dt class="col-sm-1">
            @SharedLocalizer[@Html.DisplayNameFor(model => model.Description).ToLower()]
        </dt>
        <dd class = "col-sm-11">
            <span style="padding-right : 15px">:</span>@Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ModuleId" class="btn btn-primary mb-3">
        @SharedLocalizer["edit"]
    </a> 
    <a asp-action="Index" class="btn btn-primary mb-3">
        @SharedLocalizer["goback"]
    </a>
</div>
