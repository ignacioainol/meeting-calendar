@model IEnumerable<TCS.EF.Entidades.Yarn>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.YarnCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.YarnCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.YarnId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.YarnId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.YarnId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
